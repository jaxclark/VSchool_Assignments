import React from 'react'
// import { expression } from '@babel/template'

const Square = (props) => {
    return(
        <div style={{backgroundColor: props.color, height: '200px'}}>
        </div>
    )
}

export default Square